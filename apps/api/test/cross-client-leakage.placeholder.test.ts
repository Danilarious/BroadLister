import { describe, it } from "vitest";

describe("cross-client leakage integration test", () => {
  it.todo("Phase 3: inserts two clients and fails if either client's overlay data is reachable from the other client's scoped API view");
});

